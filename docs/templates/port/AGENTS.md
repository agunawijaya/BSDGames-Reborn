# AGENTS.md — `<GAME>` `<PORT-NAME>` port

For per-game agent context, see
[`../../AGENTS.md`](../../AGENTS.md).

For repository-wide agent instructions and the Universal Port
Contract, see the root
[`AGENTS.md`](../../../../AGENTS.md).

## Port-Specific Notes

Keep this file short. Anything long-form belongs in
[`docs/decisions/`](docs/decisions/) as a port-level ADR.

- **Tech stack:** *(fill in — e.g. TypeScript + Vite, Rust +
  ratatui, Godot 4, …)*
- **Build system:** *(fill in — npm / pnpm / cargo / poetry / go
  modules / godot editor / …)*
- **Run dev server:** *(exact command, e.g. `npm run dev`)*
- **Run tests:** *(exact command, e.g. `npm test`)*
- **Build production:** *(exact command)*
- **Deploy target:** *(e.g. Vercel, itch.io, GitHub Pages, itch.io,
  APK release page)*

## Constraints Specific to This Port

*(Optional — list any conventions or gotchas that agents editing
this port should know but that would not appear in the game-level
or root AGENTS.md. Examples: "input handling in this port uses X
because of Y", "avoid feature Z which is scheduled for removal".)*
