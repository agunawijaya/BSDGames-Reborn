# CLAUDE.md

For agent instructions specific to this game, see
[`AGENTS.md`](./AGENTS.md).

For repository-wide agent instructions, see the root
[`AGENTS.md`](../../AGENTS.md).

This project uses `AGENTS.md` as the single source of truth for AI
coding agents at every folder level. This file exists only so
Claude Code auto-loads a pointer to the real instructions.
