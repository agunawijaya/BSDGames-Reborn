# Decisions — `rain`

This folder holds per-game Architecture Decision Records (ADRs) for `rain`.

Per-game ADRs are created **only** when this specific game needs to override a root default. Silence means the root defaults in [`../../../../docs/decisions/`](../../../../docs/decisions/) apply.

Currently there are no per-game ADR overrides for `rain`.
